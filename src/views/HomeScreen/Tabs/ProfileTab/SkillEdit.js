import { View, StyleSheet, Text, ScrollView, Pressable } from 'react-native'
import { useState, useEffect } from 'react';

export default function SkillEdit({ navigation }) {

    const styles = StyleSheet.create({
        view: {
            display: 'flex',
            alignItems: 'center',
        },
    })

    return (
        <View style={styles.view}>
           
        </View>
    )
}